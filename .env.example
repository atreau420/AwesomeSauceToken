# Copy to .env and fill in values

# --- Blockchain Keys (Never commit actual keys) ---
# Put your real key ONLY in a local .env (which is gitignored). Do NOT paste it into this example before committing.
PRIVATE_KEY=
WALLET_ADDRESS=

# --- RPC URLs ---
ETH_RPC_URL=https://mainnet.infura.io/v3/YOUR_KEY
POLYGON_RPC_URL=
ARBITRUM_RPC_URL=
BASE_RPC_URL=

# --- Trading Bot Risk & Performance Controls ---
# Minimum micro trade ROI in basis points (1 bps = 0.01%)
MIN_MICRO_ROI_BPS=25
# Daily gas spend cap in native ETH units
DAILY_GAS_CAP_ETH=0.05
# Default slippage tolerance in basis points
DEFAULT_SLIPPAGE_BPS=50
# Cached ETH price fallback
ETH_PRICE_USD=3000

# Adaptive Wealth Building / Micro Arbitrage Additions
ENABLE_REAL_TRADES=false
MIN_PROFIT_BPS=2
MAX_TRADE_FRACTION=0.02
BASELINE_PRESERVE_PCT=0.85
VOL_LOOKBACK=25
INVENTORY_ROTATION_BPS=1

# --- Backend Server Config ---
PORT=8080
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:3000

# --- Metrics / Observability (placeholders) ---
PROMETHEUS_PUSHGATEWAY=
SENTRY_DSN=

# --- AI Agent Intervals (ms) ---
AI_MAINTENANCE_INTERVAL_MS=300000
AI_TREASURY_INTERVAL_MS=600000
AI_GAME_INTERVAL_MS=900000

# --- WebSocket ---
WS_PORT=0

# --- Marketplace ---
MARKETPLACE_TREASURY=0xYOUR_TREASURY_WALLET_ADDRESS

# --- Security Notes ---
# 1. NEVER commit a populated PRIVATE_KEY to git.
# 2. For production set ENABLE_REAL_TRADES=true only after validating dry‑run performance.
# 3. Rotate keys immediately if they were ever exposed.
# 4. Use a dedicated low-balance hot wallet for the bot; keep treasury funds separate.

# --- Stripe / Payments (if needed) ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
